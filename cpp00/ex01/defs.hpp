#ifndef DEFS_HPP
#define DEFS_HPP

// Different enum definitions and constants are placed here.

#define IO_FAIL 1

typedef enum e_command {
    ADD,
    SEARCH,
    EXIT
} t_command;

#endif  // DEFS_HPP
